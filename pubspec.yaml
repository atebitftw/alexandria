name: alexandria
description: A utility for consolidating dart docs from multiple projects into a single site.
license: MIT
version: 1.0.6
environment:
  sdk: ^3.9.2
publish_to: none

executables:
  alexandria:

dependencies:
  args: ^2.7.0
  logging:
  path: ^1.8.0
dev_dependencies:
  lints:
